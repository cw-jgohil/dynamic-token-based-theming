/** Message shown when the grid has no rows. */
export const DATA_GRID_EMPTY_MESSAGE = "No data available";

/** Default number of rows per page when pagination is enabled. */
export const DATA_GRID_DEFAULT_PAGE_SIZE = 10;

/** Default placeholder for the search input when search is enabled. */
export const DATA_GRID_DEFAULT_SEARCH_PLACEHOLDER = "Search...";
